{
  "name": "medivoyage-ai",
  "version": "0.1.0",
  "description": "Cloudflare Worker that proxies the Medivoyage AI demo to Anthropic Claude.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
